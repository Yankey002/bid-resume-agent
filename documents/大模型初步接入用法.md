已实现内容

- 新增大模型语义层模块： resume_parser/llm_engine.py:54 的 generate_semantic_report 会基于“工作经历抽取结果 + 规则引擎报告”生成结构化语义检查报告，并内置：
  - Ollama 本地调用（ /api/generate ，强制 format="json" ）： resume_parser/llm_engine.py:21
  - JSON 解析失败自动重试一次（更强约束提示）： resume_parser/llm_engine.py:113
  - 基于可验证信号的模型路由（时间线冲突≥阈值先走推理模型）： resume_parser/llm_engine.py:157
  - 兜底模型自动降 num_ctx （14B/32B 默认降到 2048）： resume_parser/llm_engine.py:147
  - 输入脱敏（手机号/邮箱）： resume_parser/llm_engine.py:370
  - 规则致命错误（ error >0）直接返回，不调用模型： resume_parser/llm_engine.py:92
CLI 接入

- resume_parser/docx_parser.py 新增一条完整链路参数： --llm-work-experience-report （解析/抽取/规则/语义报告一把梭）
  - 参数与入口： resume_parser/docx_parser.py:88-136
  - 执行逻辑： resume_parser/docx_parser.py:159-177
怎么用

- 生成“工作经历语义与报告”（输入支持 .docx 或解析后的 .json ）：
```
conda activate base
python -m resume_parser.docx_parser "C:\path\to\resume.docx" 
--llm-work-experience-report --pretty -o report.json
```
- 可选覆盖模型与推理参数（示例）：
```
conda activate base
python -m resume_parser.docx_parser "C:\path\to\resume.docx" `
  --llm-work-experience-report `
  --ollama-base-url http://localhost:11434 `
  --llm-model-main qwen2.5:7b-instruct `
  --llm-model-reasoner deepseek-r1:7b `
  --llm-model-fallback qwen2.5:14b-instruct `
  --llm-num-ctx 4096 `
  --llm-temperature 0.1 `
  --llm-timeline-threshold 2 `
  --pretty -o report.json
```
校验

- 已做 python -m compileall resume_parser 编译校验通过，并确认 --help 正常展示新参数。
